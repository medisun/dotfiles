## 
## Bspwm
## 

# reload sxhkd config:
ctrl + alt + Escape
											pkill -USR1 -x sxhkd
super + Delete
											bspc quit

## 
## Desktop
## 

super + {less,greater}
											bspc monitor --focus {prev,next}
super + {comma,period}
											bspc desktop --focus {prev,next}
ctrl + shift + super + {comma,period}
											bspc desktop --to-monitor {prev,next}
ctrl + super + {comma,period}
											bspc desktop --swap {prev,next}
alt + super + {comma,period}
											{bspc window --to-desktop prev; bspc desktop --focus prev, bspc window --to-desktop next; bspc desktop --focus next}
ctrl + alt + super + {comma,period}
											{bspc desktop --to-monitor prev; bspc monitor --focus prev, bspc desktop --to-monitor next; bspc monitor --focus next}
super + n
											bspc desktop --balance
super + {_,shift +} Tab
											bspc desktop --focus {next,prev}
super + Home
											bspc desktop --focus last.free
super + m
											bspc desktop --layout next
super + ctrl + {_,shift + } Tab
											bspc desktop --circulate {backward,forward}
super + apostrophe
											bspc desktop --swap last
super + alt + {_,shift + }{1-9,0}
											bspc {monitor --focus, desktop --to-monitor } ^{1-9,10}
## 
## Windows
## 

super + d
											bspc control --toggle-visibility
super + b : {_,alt +,shift +,ctrl +} {Left,Down,Up,Right}
											bspc window {--focus, --to-window, --swap, --presel} {left,down,up,right}
alt + {_,shift +} F4
											bspc window --{close,kill}
super + {i,o,p,space,alt + space}
											bspc window --toggle {locked,sticky,private,floating,pseudo_tiled}
super + F11
											bspc window --toggle fullscreen
alt + {_,shift +} Tab
											bspc window --focus {next,prev}
alt + e
											bspc window --focus {last.floating,last.tiled}
alt + Escape
											bspc window --focus prev
super + Escape
											bspc window --to-desktop next
super + alt + Return
											bspc window --swap biggest
super + {_,shift + } {Left,Down,Up,Right}
											bspc window --{focus,swap} {left,down,up,right}
super + {_,shift + } {h,j,k,l}
											bspc window --{focus,swap} {left,down,up,right}
super + u
											bspc window --focus last.urgent
super + bracket{left,right}
											bspc desktop --flip {horizontal,vertical}
super + brace{left,right}
											bspc desktop --rotate {270,90}
super + ctrl + {Left,Down,Up,Right}
											bspc window --presel {left,down,up,right}
super + ctrl + {_,shift + }space
											bspc {window --presel cancel,desktop --cancel-presel}
super + alt + {Left,Down,Up,Right}
											bspc window --edge {left -10,down +10,up -10,right +10}
super + alt + shift + {Left,Down,Up,Right}
											bspc window --edge {right -10,up +10,down -10,left +10}
super + {_,shift + }{1-9,0}
											bspc {desktop --focus,window --to-desktop } ^{1-9,10}
super + {equal,minus}
											bspc config -d focused window_gap $((`bspc config -d focused window_gap` {+,-} 1 ))
super + alt + {_, shift} + equal
											bspc config -d focused right_padding  $((`bspc config -d focused right_padding` {+,-} 1 ))
super + alt + {_, shift} + minus
											bspc config -d focused left_padding   $((`bspc config -d focused left_padding` {+,-} 1 ))
super + ctrl + {_, shift} + equal
											bspc config -d focused bottom_padding $((`bspc config -d focused bottom_padding` {+,-} 1 ))
super + ctrl + {_, shift} + minus
											bspc config -d focused top_padding    $((`bspc config -d focused top_padding` {+,-} 1 ))

## 
## Mouse
## 
asdasdasddas
`
~button1
											bspc pointer --grab focus
alt + button{1-3}
											; bspc pointer --grab {move,resize_side,resize_corner}
alt + @button{1-3}
											bspc pointer --ungrab
super + button{1-3}
											; bspc pointer --grab {move,resize_side,resize_corner}
super + @button{1-3}
											bspc pointer --ungrab
alt + button{4,5}
											bspc window --focus {prev,next}
super + shift + button{4,5}
											{bspc window --to-desktop prev; bspc desktop --focus prev, bspc window --to-desktop next; bspc desktop --focus next}
super + ctrl + button{4,5}
											bspc desktop --focus {prev, next}

## 
## Applications
## 

super + v
											bspc desktop -n $( zenity --entry --text "Rename current desktop" )
super + Return
											terminator
super + alt + Escape
											/opt/scripts/killbymouse.sh
alt + F2
											/usr/bin/gmrun
alt + F3
											/home/morock/.config/dmenu/dmenu-bind.sh
super + shift + w
											zenity --question --text 'firefox stable'  && /opt/firefox/firefox -profile '/home/morock/.mozilla/firefox/stable.profile'
super + shift + g
											zenity --question --text 'smartgit'        && /opt/smartgit/bin/smartgit.sh
super + shift + x
											zenity --question --text 'mysql-workbench' && /usr/bin/mysql-workbench
super + shift + q
											zenity --question --text 'zap'             && /opt/zap/zap.sh
super + shift + r
											zenity --question --text 'filezilla'       && /usr/bin/filezilla
super + shift + a
											/usr/bin/spacefm -n
super + shift + f
											/usr/bin/thunar
super + shift + e
											/usr/bin/thunderbir
super + q
											jumpapp -c 'org-zaproxy-zap-ZAP' -i 'sun-awt-X11-XFramePeer' '/opt/zap/zap.sh'
super + w
											jumpapp '/opt/firefox/firefox'
super + t
											jumpapp -f '/usr/bin/skype'
super + e
											jumpapp -f '/usr/bin/thunderbird'
super + s
											jumpapp '/opt/sublime_text_3/sublime_text'
super + x
											jumpapp mysql-workbench
super + a
											jumpapp spacefm
super + f
											jumpapp thunar
super + g
											jumpapp -c SmartGit -i SmartGit '/opt/smartgit/bin/smartgit.sh'
super + y
											wmctrl -a 'Log for '
super + c
											wmctrl -a 'HttpRequester'
XF86Mail
											jumpapp thunderbird
super + Delete
											/home/morock/bin/logout.sh
shift + Print
											xfce4-screenshooter
super + alt + v
											( pidof pavucontrol && wmctrl -xa Pavucontrol ) || pavucontrol
super + alt + w
											jumpapp -c Chromium -i Chromium '/usr/bin/chromium'
Print
											scrot '%Y-%m-%d--%s_$wx$h_scrot.png' -e 'mv $f ~/images/ &amp; viewnior ~/images/$f'
# XF86Tools
											# urxvt -e ncmpcpp
# XF86Audio{Prev,Next,Play,Stop}
											# mpc -q {prev,next,play,pause}
# shift + XF86Audio{Prev,Next}
											# mpc seek {-,+}00:00:10
XF86Audio{Lower,Raise}Volume
											amixer -D pulse sset Master 4%{-,+} &> /dev/null
XF86AudioMute
											amixer -D pulse sset Master toggle &> /dev/null

# XF86Back
# XF86Forward
# XF86HomePage
# XF86Mail
# XF86Tools
# XF86AudioPlay
# XF86AudioStop
# XF86AudioPrev
# XF86AudioNext
# XF86AudioLowerVolume
# XF86AudioRaiseVolume
# XF86AudioMute

# DIR         := left | right | up | down
# CYCLE_DIR   := next | prev

##
## Window States
##
# floating     - Is above any tiled window and can be moved/resized freely. Although it doesn’t occupy any tiling space, it is still part of the window tree.
# pseudo_tiled - Has an unrestricted size while being centered in its tiling space.
# fullscreen   - Fills its monitor rectangle, is above all the other windows and has no borders.
# locked       - Ignores the close message.
# sticky       - Stays in the focused desktop of its monitor.
# private      - Tries to keep the same tiling position/size.
